<UserControl x:Class="HospitalManagement.RegisterTextEntryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HospitalManagement"
             xmlns:core="clr-namespace:HospitalManagement.Core;assembly=HospitalManagement.Core"
             mc:Ignorable="d"
             d:DataContext="{x:Static core:RegisterViewModel.Instance}">
   
   <!-- Grey underline -->
    <Border BorderBrush="{StaticResource ForegroundDarkBrush}"
               BorderThickness="0 0 0 0.4">
               
       <Grid Background="{StaticResource ForegroundLightBrush}">
          
          <!-- Edit Content -->
          <TextBox
                   Tag="{Binding}"
                   FontSize="{StaticResource FontSizeLarge}"
                   Padding="2"
                   Margin="10 10 15 8"/>
          

       </Grid>
       
     </Border>
</UserControl>
